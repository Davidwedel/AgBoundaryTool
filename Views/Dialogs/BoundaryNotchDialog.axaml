<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:AgBoundaryTool.ViewModels"
        x:Class="AgBoundaryTool.Views.Dialogs.BoundaryNotchDialog"
        x:DataType="vm:MainWindowViewModel"
        Title="Apply Boundary Notch"
        Width="450" Height="400"
        CanResize="false">

    <Border BorderBrush="Purple" BorderThickness="2" CornerRadius="5" Padding="10" Margin="10">
        <StackPanel Spacing="10">
            <TextBlock Text="Apply Boundary Notch" FontWeight="Bold" FontSize="16"/>

            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="Gray">
                Review your recorded notch and apply it to the boundary, or cancel to discard.
            </TextBlock>

            <!-- Notch Status -->
            <Border Background="#F0F0F0" Padding="10" CornerRadius="3">
                <StackPanel Spacing="5">
                    <TextBlock Text="{Binding NotchPointCount, StringFormat='Points Recorded: {0}'}"
                               FontSize="13"/>
                    <TextBlock Text="{Binding StatusText}"
                               TextWrapping="Wrap"
                               FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Warning if invalid -->
            <Border Background="#FFF3CD" BorderBrush="#FFA500" BorderThickness="1"
                    Padding="8" CornerRadius="3"
                    IsVisible="{Binding !CanApplyNotch}">
                <StackPanel Spacing="3">
                    <TextBlock Text="⚠ Cannot Apply Notch" FontWeight="Bold" FontSize="12" Foreground="#856404"/>
                    <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="#856404">
                        The notch path must cross the boundary at least twice, with start and end points outside the boundary.
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Spacing="8" Margin="0,10,0,0">
                <Button Content="Apply Notch to Boundary"
                        Click="ApplyButton_Click"
                        IsEnabled="{Binding CanApplyNotch}"
                        Background="Green" Foreground="White"
                        HorizontalAlignment="Stretch"
                        Height="40"
                        FontSize="14" FontWeight="Bold"/>

                <Button Content="Cancel - Discard Notch"
                        Click="CancelButton_Click"
                        Background="DarkRed" Foreground="White"
                        HorizontalAlignment="Stretch"
                        Height="40"
                        FontSize="14"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
